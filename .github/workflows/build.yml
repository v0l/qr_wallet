name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

# Cancel superseded runs on the same ref.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FLUTTER_VERSION: "3.41.6"
  FLUTTER_CHANNEL: "stable"

jobs:
  # ---------------------------------------------------------------- Analyze ---
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true
      - run: flutter pub get
      - run: dart format --output=none --set-exit-if-changed lib
      - run: flutter analyze

  # ---------------------------------------------------------------- Android ---
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true
      - run: flutter pub get
      - name: Build APK (release)
        run: flutter build apk --release
      - name: Build App Bundle (release)
        run: flutter build appbundle --release
      - name: Rename outputs
        run: |
          mv build/app/outputs/flutter-apk/app-release.apk qr-wallet-android.apk
          mv build/app/outputs/bundle/release/app-release.aab qr-wallet-android.aab
      # `archive: false` (upload-artifact v7) uploads the file un-zipped, so
      # the APK downloads straight from the run page and installs directly.
      # The artifact is named after the file; the `name` input is ignored.
      - uses: actions/upload-artifact@v7
        with:
          name: android-apk
          path: qr-wallet-android.apk
          if-no-files-found: error
          archive: false
      - uses: actions/upload-artifact@v7
        with:
          name: android-aab
          path: qr-wallet-android.aab
          if-no-files-found: error
          archive: false

  # -------------------------------------------------------------------- iOS ---
  ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true
      - run: flutter pub get
      - name: Build iOS (release, unsigned)
        run: flutter build ios --release --no-codesign
      - name: Package unsigned IPA
        run: |
          cd build/ios/iphoneos
          mkdir -p Payload
          cp -R Runner.app Payload/
          zip -r -y "$GITHUB_WORKSPACE/qr-wallet-ios-unsigned.ipa" Payload
      - uses: actions/upload-artifact@v7
        with:
          name: ios
          path: qr-wallet-ios-unsigned.ipa
          if-no-files-found: error
          archive: false

  # ------------------------------------------------------------------ macOS ---
  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true
      - run: flutter pub get
      - name: Build macOS (release)
        run: flutter build macos --release
      - name: Zip .app
        run: |
          cd build/macos/Build/Products/Release
          zip -r -y "$GITHUB_WORKSPACE/qr-wallet-macos.zip" "QR Wallet.app"
      - uses: actions/upload-artifact@v7
        with:
          name: macos
          path: qr-wallet-macos.zip
          if-no-files-found: error
          archive: false

  # ------------------------------------------------------------------ Linux ---
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Linux build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config \
            libgtk-3-dev liblzma-dev libstdc++-12-dev
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true
      - run: flutter pub get
      - name: Build Linux (release)
        run: flutter build linux --release
      - name: Tar bundle
        run: |
          cd build/linux/x64/release/bundle
          tar czf "$GITHUB_WORKSPACE/qr-wallet-linux-x64.tar.gz" .
      - uses: actions/upload-artifact@v7
        with:
          name: linux
          path: qr-wallet-linux-x64.tar.gz
          if-no-files-found: error
          archive: false

  # ---------------------------------------------------------------- Windows ---
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true
      - run: flutter pub get
      - name: Build Windows (release)
        run: flutter build windows --release
      - name: Zip bundle
        shell: pwsh
        run: |
          Compress-Archive -Path build/windows/x64/runner/Release/* `
            -DestinationPath qr-wallet-windows-x64.zip
      - uses: actions/upload-artifact@v7
        with:
          name: windows
          path: qr-wallet-windows-x64.zip
          if-no-files-found: error
          archive: false

  # -------------------------------------------------------------------- Web ---
  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true
      - run: flutter pub get
      - name: Build Web (release)
        run: flutter build web --release
      - name: Tar bundle
        run: tar czf "$GITHUB_WORKSPACE/qr-wallet-web.tar.gz" -C build/web .
      - uses: actions/upload-artifact@v7
        with:
          name: web
          path: qr-wallet-web.tar.gz
          if-no-files-found: error
          archive: false

  # ---------------------------------------------------- Release (on tags) -----
  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [android, ios, macos, linux, windows, web]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist
          # Our .zip/.tar.gz bundles are artifacts in their own right; don't
          # let the action unpack them.
          skip-decompress: true
      - name: Flatten
        run: |
          mkdir -p out
          find dist -type f -exec mv {} out/ \;
          ls -la out
      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: out/*
          generate_release_notes: true

  # ------------------------------------- Rolling nightly release (on main) ----
  # Convenience: stable download URLs for the latest `main` build.
  nightly:
    if: github.ref == 'refs/heads/main'
    needs: [android, ios, macos, linux, windows, web]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v8
        with:
          path: dist
          # Our .zip/.tar.gz bundles are artifacts in their own right; don't
          # let the action unpack them.
          skip-decompress: true
      - name: Flatten
        run: |
          mkdir -p out
          find dist -type f -exec mv {} out/ \;
          ls -la out
      - name: Delete stale nightly assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view nightly --json assets -q '.assets[].name' 2>/dev/null |
            while read -r a; do gh release delete-asset nightly "$a" -y; done || true
      - name: Move nightly tag
        run: |
          git tag -f nightly
          git push -f origin nightly
      - name: Publish rolling pre-release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly
          name: Nightly (${{ github.sha }})
          body: |
            Automatic build of `main` @ ${{ github.sha }}.

            Download `qr-wallet-android.apk` and install it directly.
          prerelease: true
          make_latest: false
          files: out/*
